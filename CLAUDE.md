# CLAUDE.md - Google Classroom Manager

> **AI Agent 為核心的課程管理工具**  
> 透過 Cursor + Claude 對話，完成所有 Google Classroom 操作

## 🤖 工作模式

```
📝 使用者描述需求（自然語言）
    ↓
🔧 AI 修改程式碼 + clasp push
    ↓
▶️ 使用者到 GAS 執行函數
    ↓
📋 使用者貼回結果
    ↓
🔄 AI 分析 + 調整
```

## 📁 專案結構

```
├── CLAUDE.md              # AI 記憶體
├── data/
│   ├── teachers.json      # 老師資料庫
│   ├── courses.json       # 課程分類
│   └── school-years/      # 學年設定
├── workflows/             # 可重用工作流程
└── src/                   # GAS 程式碼
    ├── SimpleCourseCreator.js  # 主要功能
    ├── ClassroomService.js     # API 封裝
    ├── RateLimiter.js          # 限速器
    └── Code.js                 # 入口點
```

## ⚠️ 執行限制

| 限制 | 數值 | 解決方式 |
|------|------|----------|
| 執行時間 | 6 分鐘 | 分批處理 |
| API 配額 | 50/分鐘 | 智能限速 |

## 👤 帳號配置

| 帳號 | 用途 |
|------|------|
| `tsehungchen@kcislk.ntpc.edu.tw` | 執行腳本 |
| `lkclassle114@kcislk.ntpc.edu.tw` | 課程擁有者 |

## 🚀 常用指令

```bash
clasp push --force   # 部署程式碼
clasp open           # 開啟 GAS 編輯器
```

## 📚 可用函數

| 函數 | 用途 |
|------|------|
| `previewKCFSCourses` | 預覽 KCFS 課程 |
| `addTeacherToKCFSCourses` | 新增老師到 KCFS |
| `quickTestAddTeacher` | 快速測試權限 |
| `createMyPalCourse` | 建立 myPal 課程 |

## 📜 執行歷史

### 2024-12-22
- ✅ 新增老師到 84 個 KCFS 課程
- ✅ 移除老師從 19 個舊課程
- ✅ 建立 AI Agent 架構

---
*Powered by Cursor + Claude | 對話即開發*
